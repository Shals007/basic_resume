body {
    margin: 20px;
  }
  body {
    /* Replace 'your-image-url.jpg' with the actual URL or relative path of your image */
    background-image: url('images.jfif');
    opacity:0.3
    /* Optionally, you can set the background size and position */
    background-size: 100px; /* or 'contain' or specific width and height (e.g., 100px 200px) */
    background-position: ::-ms-fill; /* or specific position (e.g., 50% 30%) */
    /* Optionally, you can set background repeat behavior */
    background-repeat: no-repeat; /* 'repeat', 'repeat-x', 'repeat-y', or 'no-repeat' */
  }
